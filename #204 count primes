class Solution {
public:
 int countPrimes(int n){
if(n < 2){
    return 0;
}

vector<bool> isnotprime(n , false);

int count = 0;
for(int i = 2; i < n; i++){
if(!isnotprime[i]){
count = count + 1;
for(int m = i*2 ; m< n; m+=i){
    isnotprime[m] = true;
}


}
}

        return count;
    }
};
